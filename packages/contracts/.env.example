# Base Sepolia deployment — source this before running forge script.
# The deployer key is imported into Foundry's keystore (see README), so it is
# NOT stored here; only the RPC and verification key live in env.
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org
ETHERSCAN_API_KEY=your_etherscan_api_key
